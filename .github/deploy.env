# ⚠️  IMPORTANTE: Este arquivo contém variáveis de ambiente de exemplo
# Nunca commite credenciais reais! Use variáveis de ambiente seguras.
# Para produção, configure estas variáveis no seu provedor de CI/CD

VPS_HOST=${VPS_HOST:-seu-servidor.com}
VPS_USER=${VPS_USER:-usuario}
VPS_PASSWORD=${VPS_PASSWORD:-sua-senha-segura}
VPS_PORT=${VPS_PORT:-22}

# Application domain and SSL email (optional)
DOMAIN=${DOMAIN:-seu-dominio.com}
LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL:-seu-email@exemplo.com}

# Database (local Postgres defaults created by deploy script)
DB_NAME=${DB_NAME:-movemarias}
DB_USER=${DB_USER:-assist_user}
DB_PASS=${DB_PASS:-sua-senha-db-segura}

# App users (defaults aligned with CI smokes)
SUPERADMIN_EMAIL=${SUPERADMIN_EMAIL:-admin@exemplo.com}
SUPERADMIN_PASSWORD=${SUPERADMIN_PASSWORD:-sua-senha-admin-segura}
ADMIN_EMAIL=${ADMIN_EMAIL:-admin@exemplo.com}
ADMIN_PASSWORD=${ADMIN_PASSWORD:-sua-senha-admin-segura}

# API port
API_PORT=${API_PORT:-3000}
